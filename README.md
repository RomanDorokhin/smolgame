SmolGame — **мини-игры в Telegram**: вертикальная лента, как короткие ролики, только вместо видео — **браузерные HTML5-игры**. Свайп — следующая игра, **«Играть»** — во весь экран, на авторов можно подписаться. **Сейчас** нет обязательных «посмотри рекламу, потом играй» — так задумано; свою игру выложи по ссылке или через GitHub (вкладка **«Загрузить»** внизу). Подписки для игроков/разработчиков, донаты и аккуратная монетизация ленты — в планах.

**Лента:** свайп по экрану переключает игры; превью и текст — в оверлее «discovery». Полноэкранная игра — по кнопке **«Играть»** (режим фокуса: без нижнего навбара и боковых действий; выход — **«← Лента»**).

**Игры:** отдельная вкладка с двумя блоками — **лайкнутые** и **недавно играл** (нужен задеплоенный бэкенд с таблицей `user_game_plays` и маршрутом `/api/me/played-games`).

**Профиль** в первую очередь для разработчика (мои опубликованные игры, модерация для админа).

**Спека для авторов HTML5-игр под ленту** (не док по коду платформы): [docs/DEVELOPER_GUIDE.md](docs/DEVELOPER_GUIDE.md) · **модерация и доработки**: [docs/MODERATION_AND_AUTHORS.md](docs/MODERATION_AND_AUTHORS.md)

Основные возможности:

- Запуск игр в WebView Telegram (iframe)
- Поддержка типичных браузерных HTML5-игр (Canvas, Phaser, чистый JS и т.д.)
- Загрузка по **ссылке** (URL) или через **GitHub**; вкладка **Премиум** пока заглушка
- Автоматический деплой фронтенда через GitHub Pages
- Бэкенд на Cloudflare Workers + D1

Проект создан для быстрого и вирусного игрового опыта: минимум барьеров для игроков и простая публикация для разработчиков.
Хочешь стать первым? Загружай свою игру — и она сразу появится в ленте.

## 🏗️ Архитектура
Проект организован по модульному принципу для обеспечения долговечности и простоты поддержки:

```
smolgame/
├── index.html, styles.css    — главная страница и стили
├── js/                       — фронтенд логика (Vanilla JS)
│   ├── core/                 — ядро (config, state, api, telegram)
│   ├── features/             — фичи (feed, profile, upload, admin)
│   ├── ui/                   — интерфейс (screens, overlays, components)
│   └── modules/              — вспомогательные системы (events, actions)
├── agent/                    — AI Архитектор (React/Vite)
├── backend/                  — API на Cloudflare Workers + D1
└── docs/                     — документация
    └── ARCHITECTURE.md        ← Подробное описание устройства проекта
```

Подробное описание слоев и жизненного цикла приложения: [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)

---

## 🚀 Как обновляются изменения

> Короткий ответ: **фронт обновляется сам, бэк — одной командой.**

Когда я правлю код и пушу в `main`:

### Если менялся только фронт (`index.html`, `styles.css`, `js/*`, картинки)

**Ничего делать не надо.** GitHub Pages видит пуш в `main` и автоматически выкатывает новую версию сайта. Обычно это занимает 30–90 секунд.

На телефоне в Telegram может кэшироваться — достаточно **свайпнуть мини-апп сверху вниз** (pull-to-refresh) или закрыть/открыть заново.

### Если менялся бэкенд (`backend/**`)

**Нужно одной командой выкатить на Cloudflare.** Делается у тебя на Маке:

```bash
cd smolgame/backend
git pull                 # подтянуть мои свежие изменения
npm run deploy           # залить на Cloudflare
```

Это ~5 секунд. Всё.

### Если менялась схема БД (`backend/schema.sql`)

Редкий случай. Тогда дополнительно:

```bash
npm run db:migrate:remote
```

### Как понять что менялось?

Я в каждом ответе пишу что трогал. Если упомянул файлы из `backend/` — нужно `npm run deploy`. Если только `js/`, `styles.css`, `index.html` — ничего делать не нужно, ждёшь минуту и обновляешь мини-апп.

**Если вдруг забыл — не страшно**, просто запусти `npm run deploy`, ничего не сломается.

---

## Включить GitHub Pages (один раз)

1. Settings → Pages
2. Source: **Deploy from a branch**
3. Branch: `main`, folder `/` (root)
4. Save

Сайт будет на `https://romandorokhin.github.io/smolgame/`.

## Бэкенд — первая настройка

См. [`backend/README.md`](backend/README.md).
